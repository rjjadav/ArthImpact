var mCtrls = require('./../_mCtrls');

mCtrls.controller('LenderController',LenderController);

LenderController.$inject = [];

function LenderController(){
	var lender = this;
	console.log("LenderController");
}